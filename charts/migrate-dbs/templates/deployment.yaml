apiVersion: batch/v1
kind: Job
metadata:
  name: migrate-dbs
spec:
  template:
    spec:
      containers:
        - name: migrate-dbs
          image: "busybox"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/bin/sh", "-c", "echo Start; sleep 10; echo End"]
